import UploadCard from './components/UploadCard'

function App() {
  return (
    <div
      className="min-h-screen w-full flex items-center justify-center"
      style={{
        background:
          'radial-gradient(ellipse at 50% 0%, #0f1744 0%, #050a1a 55%, #000000 100%)',
      }}
    >
      <UploadCard />
    </div>
  )
}

export default App
